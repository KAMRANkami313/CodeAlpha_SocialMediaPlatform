export const API_BASE_URL = 'http://localhost:5000/api';

export const STORAGE_KEYS = {
  TOKEN: 'token',
  USER: 'user',
  THEME: 'theme',
  RECENT_SEARCHES_PREFIX: 'recent_searches_'
};

export const POLL_INTERVALS = {
  NOTIFICATIONS: 10000,
  UNREAD_MESSAGES: 10000,
  MESSAGES: 3000,
  HEARTBEAT: 30000
};

export const ACTIVITY_THRESHOLD_MS = 60000;

export const SEARCH_DEBOUNCE_MS = 300;

export const TOAST_DURATION_MS = 3000;

export const STORY_PROGRESS = {
  INCREMENT: 2,
  INTERVAL_MS: 100
};

export const RECENT_SEARCHES_LIMIT = 5;

export const SUGGESTIONS_LIMIT = 5;